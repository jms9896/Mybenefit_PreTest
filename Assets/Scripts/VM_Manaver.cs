using UnityEngine;

public class VM_Manager : MonoBehaviour
{
    // TODO: VM_UI 참조 (MonoBehaviour라 인스펙터 할당)

    // TODO: 부트스트랩(조립) — EventBus 생성 후 각 모듈 생성·연결만
    //       !! 런타임 연산에는 개입하지 않는다 (조립 전용)
}
